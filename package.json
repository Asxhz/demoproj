{
  "name": "lumen-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "Lumen — a tiny static storefront demo (intentional, obvious bug for the TwoCustomer FDE agent to fix).",
  "scripts": {
    "build": "echo \"static site — nothing to build\""
  }
}
